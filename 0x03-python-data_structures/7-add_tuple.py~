#!/usr/bin/python3
# printing from a list
# Zafrullah Idris

def add_tuple(tuple_a=(), tuple_b=()):
    """adds 2 tuples."""
    left = tuple_a[0] + tuple_b[0]
    right = tuple_a[1] + tuple_b[1]
    return (left, right)
